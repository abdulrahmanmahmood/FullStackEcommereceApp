import React from 'react'

const ProductList = ({procuctaList}) => {
  return (
    <div>
      {/* {procuctaList.map((product)=>(<h1>{product}</h1>))} */}
    </div>
  )
}

export default ProductList